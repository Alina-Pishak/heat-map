import styled from "styled-components";

export const EmptyMapImg = styled.img`
  position: "relative";
`;

export const HetMapImg = styled.img`
  position: absolute;
  top: 0;
  left: 0;
  opacity: 0.3;
`;

export const FileUploadWrapper = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  background: #000;
`;
